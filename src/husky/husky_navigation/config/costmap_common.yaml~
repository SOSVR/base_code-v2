footprint: [[-0.32, -0.2], [-0.32, 0.2], [0.32, 0.2], [0.32, -0.2]]
footprint_padding: 0.05
global_frame: /map
robot_base_frame: base_link
update_frequency: 4.0
publish_frequency: 3.0
transform_tolerance: 0.5

resolution: 0.05

obstacle_range: 6.5
raytrace_range: 7.0

#layer definitions
static:
    map_topic: /map
    subscribe_to_updates: true

obstacles_laser:
    obstacle_range: 6.5
    raytrace_range: 7.5
    observation_sources: laser
    laser: {data_type: LaserScan, clearing: true, marking: true, topic: base_scan, inf_is_valid: true, sensor_frame: base_laser_link}
    track_unknown_space: true

inflation:
    inflation_radius: 1.2
    cost_scaling_factor: 2.58
